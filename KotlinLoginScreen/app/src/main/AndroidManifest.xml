<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="dor.samet.com.kotlinloginscreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".KotlinLoginApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.login_screen.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="dor.samet.com.kotlinloginscreen.presentation.next_screen.NextActivity">
        </activity>
    </application>

</manifest>